import "./globals.css";
import { GlobalStateProvider } from "../Context/page";
import { AuthProvider } from "../Context/auth";
import { FluentCProvider } from "../Context/fluentc";
import DOMPurify from 'isomorphic-dompurify';
export const metadata = {
  title: "Sustainext",
  description: "Generated by create next app",
};
export default function RootLayout({ children }) {
  const unsafeHTML = "<div>This is a browser feature intended for developers. Do not enter or paste code which you don't understand. It may allow attackers to steal your information or impersonate you.See https://en.wikipedia.org/wiki/Self-XSS for more details</div><script>alert('XSS');</script>";
  const cleanHTML = DOMPurify.sanitize(unsafeHTML);
  console.log('Sanitized HTML:', cleanHTML);
  return (
    <html lang="en">
      <head>
      <link rel="preconnect" href="https://fonts.googleapis.com"></link>
        <link
          href="https://fonts.googleapis.com/css2?family=Manrope:wght@200..800&display=swap"
          rel="stylesheet"
        ></link>

        {/* <script src="https://widget.fluentc.io/fluentcWidget.min.js"></script> */}
        <script src="https://widget.fluentc.io/fluentcWidgetV2.min.js"></script>
      </head>
      <body className="min-h-[100vh]">
        <GlobalStateProvider>
          <FluentCProvider>
            <AuthProvider>
              {/* Render the sanitized HTML */}
            
              {children}
            </AuthProvider>
          </FluentCProvider>
        </GlobalStateProvider>
      </body>
    </html>
  );
}
